# gitleaks 設定 — https://github.com/gitleaks/gitleaks
# repo ルートで自動検出。デフォルトルールを継承する。

[extend]
useDefault = true

[allowlist]
description = "雛形のプレースホルダを許可（実値は .dev.vars / wrangler secrets で管理し非コミット）"
paths = [
  '''\.dev\.vars\.example$''',
  '''\.env\.example$''',
]
