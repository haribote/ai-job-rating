# npm サプライチェーン防御（npm 11+）
# 依存の lifecycle スクリプト（postinstall 等）を遮断。必要な依存は個別に許可する
ignore-scripts=true
# exact バージョンで固定（^/~ レンジを使わない）
save-exact=true
# 公開7日未満のバージョンを除外（内部で before 日付に変換。before とは併用不可）
min-release-age=7
# engines フィールドを強制
engine-strict=true
# funding 出力を抑制
fund=false
